```bash
dir -> ver diretório
ping -n 4 google.com -> pingar o google.com 4 vezes
ipconfig -> mostra informação dos IPs, subnets e gateways
cd / -> change directory
Get-Alias -> retorna informação de alguns aliases
	Get-Alias cd
	Get-Alias dir
netstat -> exibe conexões de rede TCP/IP
	netstat -abno
Clear-RecycleBin --> limpar lixeira
```